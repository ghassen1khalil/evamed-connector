package fr.has.evamed.connector.repository;

import fr.has.evamed.connector.domain.RefRole;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RefRoleRepository extends JpaRepository<RefRole, String> {
}
