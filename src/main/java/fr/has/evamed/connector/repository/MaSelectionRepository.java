package fr.has.evamed.connector.repository;

import fr.has.evamed.connector.domain.MaSelection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MaSelectionRepository extends JpaRepository<MaSelection, Long> {
}
